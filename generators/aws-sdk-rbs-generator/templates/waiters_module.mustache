{{warning_comment}}
module Aws
  module {{service_name}}
    module Waiters
      {{#waiters}}
      class {{class_name}}
        def initialize: (client: Client, ?max_attempts: Integer, ?delay: Integer, ?before_attempt: Proc, before_wait: Proc) -> void
        def wait: ({{params}}) -> {{returns}}
      end
      {{/waiters}}
    end
  end
end
