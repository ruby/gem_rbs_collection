module Roo
  module Formatters
    module Matrix
      # returns a matrix object from the whole sheet or a rectangular area of a sheet
      def to_matrix: (?Integer? from_row, ?Integer? from_column, ?Integer? to_row, ?Integer? to_column, ?(String | Integer) sheet) -> untyped # TODO add type Matrix
    end
  end
end
